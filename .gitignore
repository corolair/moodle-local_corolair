local_corolair.zip
local_corolair-prod.zip
local_corolair/
.package-prod/
.codex
# Claude Code: per-user settings and agent worktrees (full checkouts of this repo).
.claude/
.DS_Store
plans

# Composer dev dependencies for the native phpcs run; rebuilt by `composer install`.
vendor/
